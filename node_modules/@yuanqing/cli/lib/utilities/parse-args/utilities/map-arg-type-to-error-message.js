"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapArgTypeToErrorMessage = void 0;
const stringify_enum_1 = require("./stringify-enum");
function mapArgTypeToErrorMessage(type, name) {
    if (typeof type === 'string') {
        switch (type) {
            case 'BOOLEAN': {
                return `${name} must be one one of 'false', 'true', '0', or '1'`;
            }
            case 'NUMBER': {
                return `${name} must be a number`;
            }
            case 'POSITIVE_NUMBER': {
                return `${name} must be a positive number`;
            }
            case 'NON_ZERO_POSITIVE_NUMBER': {
                return `${name} must be a non-zero positive number`;
            }
            case 'INTEGER': {
                return `${name} must be an integer`;
            }
            case 'POSITIVE_INTEGER': {
                return `${name} must be a postive integer`;
            }
            case 'NON_ZERO_POSITIVE_INTEGER': {
                return `${name} must be a non-zero positive integer`;
            }
            case 'STRING': {
                return `${name} must be a string`;
            }
            default: {
                throw new Error('Invalid type');
            }
        }
    }
    if (typeof type === 'function') {
        return `${name} expects a value`;
    }
    return `${name} must be one of ${stringify_enum_1.stringifyEnum(type)}`;
}
exports.mapArgTypeToErrorMessage = mapArgTypeToErrorMessage;
