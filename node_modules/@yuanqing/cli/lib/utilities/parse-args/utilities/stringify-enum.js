"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.stringifyEnum = void 0;
function stringifyEnum(values) {
    const strings = [];
    for (const value of values) {
        strings.push(`'${value}'`);
    }
    return `${strings.slice(0, strings.length - 1).join(', ')} or ${strings[strings.length - 1]}`;
}
exports.stringifyEnum = stringifyEnum;
