"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseInteger = void 0;
const regexes_1 = require("./regexes");
function parseInteger(arg, name) {
    const number = parseFloat(arg);
    if (regexes_1.integerRegex.test(arg) === false || isNaN(number) === true) {
        throw new Error(`${name} must be an integer but got '${arg}'`);
    }
    return number;
}
exports.parseInteger = parseInteger;
