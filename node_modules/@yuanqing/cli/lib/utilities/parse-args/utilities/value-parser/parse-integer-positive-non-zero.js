"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseIntegerPositiveNonZero = void 0;
const regexes_1 = require("./regexes");
function parseIntegerPositiveNonZero(arg, name) {
    const number = parseFloat(arg);
    if (regexes_1.zeroRegex.test(arg) === true ||
        regexes_1.positiveIntegerRegex.test(arg) === false ||
        isNaN(number) === true) {
        throw new Error(`${name} must be a non-zero positive integer but got '${arg}'`);
    }
    return number;
}
exports.parseIntegerPositiveNonZero = parseIntegerPositiveNonZero;
