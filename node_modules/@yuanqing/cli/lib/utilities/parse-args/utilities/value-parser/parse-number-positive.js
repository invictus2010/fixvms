"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseNumberPositive = void 0;
const regexes_1 = require("./regexes");
function parseNumberPositive(arg, name) {
    const number = parseFloat(arg);
    if ((regexes_1.positiveNumberRegex.test(arg) === false &&
        regexes_1.positiveIntegerRegex.test(arg) === false) ||
        isNaN(number) === true) {
        throw new Error(`${name} must be a positive number but got '${arg}'`);
    }
    return number;
}
exports.parseNumberPositive = parseNumberPositive;
